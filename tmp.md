This is why I don't play for the static website inside of the docs folder the ability or the list of sources selection should be below the search button because the idea is to have a default mode of well default selected sources that tend to always work and then have the ability to let the user change the source that they want to use if they would so like to but by default just have them all enabled and then have everything below the search button so the user can just type in their query and press search does this make sense? right like it should be the query entry box like what we already have and then the selection between the PDF or the web search or the database search or the scholar search and then right below that should be the actual search button to enact everything if that makes sense.